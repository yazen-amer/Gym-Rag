# GymRAG — Frontend

Vite + React + TypeScript + Tailwind CSS v4 scaffolding.

This is a bare starting point — see [`../plan.md`](../plan.md) for what to build here.

```bash
cd frontend
npm install
npm run dev    # http://localhost:5173
```
