# GymRAG — Backend

Python backend scaffolding, managed with [uv](https://docs.astral.sh/uv/).

This is a bare starting point — see [`../plan.md`](../plan.md) for what to build here.

```bash
cd backend
uv run main.py    # sanity check: prints a hello message
```
